// default params

function foo(name = 'John', age = 20, countryCode = 'NZ') {
  console.log(name, age, countryCode);
}

// concat

let firstName = 'Peter';
let lastName = 'McClam';
let fullName = `${firstName} ${lasttName}`;

